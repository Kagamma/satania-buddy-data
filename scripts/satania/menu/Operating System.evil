if os = 'windows'
  talk('You are using <font color="#0000ff">Windows</font>!')
else if os = 'linux'
  talk('You are using <font color="#0000ff">Linux</font>!')
else if os = 'darwin'
  scheme_load('itoddlers-btfo.evil')
else
  talk('You are using something niche, aren\'t you?')
